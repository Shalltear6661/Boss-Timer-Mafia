[build]
builder = "nixpacks"
buildCommand = "npm install --include=dev && npm run build"

[deploy]
# Build ulang saat start agar dist/assets pasti ada (hindari index.html orphan tanpa assets)
startCommand = "npm run build && node server.mjs"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
