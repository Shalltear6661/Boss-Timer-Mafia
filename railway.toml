[build]
builder = "nixpacks"
# Pastikan Vite ter-install meski NODE_ENV=production
buildCommand = "npm install --include=dev && npm run build"

[deploy]
startCommand = "npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
